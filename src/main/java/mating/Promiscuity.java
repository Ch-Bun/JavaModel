package mating;

import java.util.List;

import creature.Creature;

public class Promiscuity extends MatingSystem {

	@Override
	public void mating(List<Creature> femalePool, List<Creature> malePool) {
//		// TODO Auto-generated method stub
//		double maxFecundity = LifeStageFactory.getLifeStage(female.getStage(), Sex.FEMALE).getDemography().getFecundity();
//		//basically female carries sperm from males chosen from pool of males in patch, fathers limited by max offspring, i.e. each baby has a different dad 
//		int maxNumberOfMates= Math.min((int) Math.round(maxFecundity), matingMales.size());
//		for(int i = 0; i < maxNumberOfMates; i++) {
//			Creature mate = matingMales.get(RandomNumberGenerator.getRandomIndex(matingMales.size()));
//			female.addMate(mate);
//		}
//	}
	}

}
